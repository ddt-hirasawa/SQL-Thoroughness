演習8-2 クエリをALL述語で書き換えよ

select *

from Projects P1

where '○' = ALL(

select case when step_nbr <= 1 AND status = '完了' then '○'

when step_nbr > 1  AND status = '待機' then '○'

else '×' END

from Projects P2

where P1.project_id = P2. project_id);